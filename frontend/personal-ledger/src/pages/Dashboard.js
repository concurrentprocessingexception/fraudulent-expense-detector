import React, { useEffect, useState } from "react";

const Dashboard = () => {
  
  return (
    <div>
      <h2>Expense ledger</h2>

    </div>
  );
};

const styles = {
  table: {
    width: "100%",
    background: "white",
    borderRadius: "8px",
    overflow: "hidden",
  },
  row: {
    cursor: "pointer",
    transition: "0.2s"
  },
};

export default Dashboard;